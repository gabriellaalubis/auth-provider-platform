export function renderAdminUi(): string {
  return `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>SSO Control Panel</title>
  <style>
    :root{--navy:#16233a;--navy-soft:#22314c;--slate:#5a6779;--line:#dde3ec;--bg:#f4f6fa;--white:#fff;--accent:#2b5fd9;--accent-dark:#1e47a8;--green:#18794e;--green-bg:#e6f6ed;--red:#b42318;--red-bg:#feeceb;--amber:#8a6100;--amber-bg:#fff4d6;--radius:10px;--shadow:0 1px 3px rgba(20,30,50,.12)}
    *{box-sizing:border-box}body{margin:0;font:14px/1.5 -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:var(--navy);background:var(--bg)}button,input,select,textarea{font:inherit}button{cursor:pointer}h1,h2,h3,p{margin-top:0}.muted{color:var(--slate)}.small{font-size:12px}.mono{font-family:Consolas,monospace}.hidden{display:none!important}
    .layout{display:grid;grid-template-columns:248px 1fr;min-height:100vh}.sidebar{position:sticky;top:0;height:100vh;padding:26px 18px;background:var(--navy);color:#fff}.brand{padding:0 12px 25px;font-size:18px;font-weight:750}.brand span{display:block;margin-top:4px;font-size:11px;font-weight:600;letter-spacing:.12em;text-transform:uppercase;color:#aebbd0}.nav{display:grid;gap:7px}.nav button{border:0;border-radius:8px;padding:11px 13px;text-align:left;color:#cbd5e5;background:transparent}.nav button:hover,.nav button.active{color:#fff;background:var(--navy-soft)}.main{min-width:0}.topbar{height:76px;padding:0 32px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid var(--line);background:#fff}.topbar h1{margin:0;font-size:20px}.content{padding:28px 32px;max-width:1500px}.section{display:none}.section.active{display:block}.grid{display:grid;gap:16px}.metrics{grid-template-columns:repeat(4,minmax(0,1fr));margin-bottom:20px}.card{background:#fff;border:1px solid var(--line);border-radius:var(--radius);box-shadow:var(--shadow);padding:22px}.metric-label{color:var(--slate);font-size:12px;font-weight:700;text-transform:uppercase;letter-spacing:.08em}.metric-value{font-size:30px;font-weight:750;margin:6px 0}.card-header,.row{display:flex;align-items:center;justify-content:space-between;gap:14px}.card-header{margin-bottom:18px}.card-header h2{margin:0;font-size:19px}.btn{border:1px solid #cbd5e1;border-radius:7px;padding:9px 13px;color:var(--navy);background:#fff;font-weight:650}.btn:hover{background:#f8fafc}.btn-primary{border-color:var(--accent);color:#fff;background:var(--accent)}.btn-primary:hover{background:var(--accent-dark)}.btn-danger{border-color:#efc3bf;color:var(--red);background:#fff}.btn-sm{padding:6px 9px;font-size:12px}.toolbar{display:flex;flex-wrap:wrap;gap:9px;margin-bottom:16px}.input,select,textarea{width:100%;border:1px solid #cbd5e1;border-radius:7px;padding:9px 11px;color:var(--navy);background:#fff}.toolbar .input,.toolbar select{width:auto;min-width:190px}.table-wrap{overflow:auto}.data-table{width:100%;border-collapse:collapse}.data-table th,.data-table td{padding:13px 12px;border-bottom:1px solid var(--line);text-align:left;vertical-align:middle}.data-table th{font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:var(--slate);background:#f0f3f7}.actions{display:flex;flex-wrap:wrap;gap:6px}.badge{display:inline-block;border-radius:999px;padding:4px 9px;font-size:12px;font-weight:700}.active-badge{color:var(--green);background:var(--green-bg)}.inactive-badge{color:var(--red);background:var(--red-bg)}.neutral-badge{color:#40516d;background:#edf1f6}.empty{text-align:center!important;color:var(--slate);padding:30px!important}.modal-backdrop{position:fixed;inset:0;z-index:20;display:none;align-items:center;justify-content:center;padding:20px;background:rgba(15,23,42,.55)}.modal-backdrop.open{display:flex}.modal{width:min(620px,100%);max-height:90vh;overflow:auto;border-radius:12px;background:#fff;box-shadow:0 24px 60px rgba(15,23,42,.3);padding:22px}.modal-header{display:flex;align-items:center;justify-content:space-between;margin-bottom:18px}.modal-header h2{margin:0}.close{border:0;background:transparent;font-size:25px;color:var(--slate)}.field{margin-bottom:15px}.field label{display:block;margin-bottom:6px;font-weight:700}.field-help{margin-top:5px;color:var(--slate);font-size:12px}.modal-footer{display:flex;justify-content:flex-end;gap:9px;margin-top:20px}.checklist{display:grid;gap:8px}.check-row{display:flex;align-items:center;justify-content:space-between;gap:12px;border:1px solid var(--line);border-radius:8px;padding:10px}.check-row label{display:flex;align-items:center;gap:9px}.alert{position:fixed;right:20px;bottom:20px;z-index:40;max-width:420px;border-radius:9px;padding:13px 16px;color:#fff;background:var(--navy);box-shadow:0 8px 30px rgba(15,23,42,.25)}.alert.error{background:var(--red)}.secret{padding:12px;border-radius:8px;color:var(--amber);background:var(--amber-bg);overflow-wrap:anywhere}.loading{opacity:.55;pointer-events:none}
    @media(max-width:900px){.layout{grid-template-columns:1fr}.sidebar{position:static;height:auto}.nav{grid-template-columns:repeat(4,1fr)}.nav button{text-align:center}.metrics{grid-template-columns:repeat(2,1fr)}.topbar,.content{padding-left:18px;padding-right:18px}}
    @media(max-width:560px){.nav{grid-template-columns:1fr 1fr}.metrics{grid-template-columns:1fr}.topbar .muted{display:none}.content{padding-top:18px}.card{padding:15px}}
  </style>
</head>
<body>
<div class="layout">
  <aside class="sidebar"><div class="brand">Admin SSO<span>Control Panel</span></div><nav class="nav"><button class="active" data-section="dashboard">Dashboard</button><button data-section="users">Users</button><button data-section="groups">Groups</button><button data-section="applications">Applications</button></nav></aside>
  <main class="main">
    <header class="topbar"><h1 id="page-title">Dashboard</h1><span class="muted">Identity and access administration</span></header>
    <div class="content" id="content">
      <section id="dashboard" class="section active"><div class="grid metrics"><div class="card"><div class="metric-label">Active users</div><div class="metric-value" id="active-users">—</div><div class="small muted" id="total-users">Loading users</div></div><div class="card"><div class="metric-label">Groups</div><div class="metric-value" id="group-count">—</div><div class="small muted">Access groups</div></div><div class="card"><div class="metric-label">Active applications</div><div class="metric-value" id="active-apps">—</div><div class="small muted" id="total-apps">Loading applications</div></div><div class="card"><div class="metric-label">System status</div><div class="metric-value" style="font-size:22px;color:var(--green)">Operational</div><div class="small muted">Control Panel API is available</div></div></div><div class="card"><div class="card-header"><div><h2>Access overview</h2><div class="small muted">Current application policies</div></div></div><div id="policy-overview"></div></div></section>
      <section id="users" class="section"><div class="card"><div class="card-header"><div><h2>Users</h2><div class="small muted">Create accounts and manage their status and credentials.</div></div><button class="btn btn-primary" id="add-user">Add user</button></div><div class="toolbar"><input class="input" id="user-search" placeholder="Search by name or email"><select id="user-filter"><option value="">All statuses</option><option value="ACTIVE">Active</option><option value="INACTIVE">Inactive</option></select></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Name</th><th>Email</th><th>Status</th><th>Created</th><th>Actions</th></tr></thead><tbody id="user-rows"></tbody></table></div></div></section>
      <section id="groups" class="section"><div class="card"><div class="card-header"><div><h2>Groups</h2><div class="small muted">Organize users into reusable access groups.</div></div><button class="btn btn-primary" id="add-group">Add group</button></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Group</th><th>Members</th><th>Created</th><th>Actions</th></tr></thead><tbody id="group-rows"></tbody></table></div></div></section>
      <section id="applications" class="section"><div class="card"><div class="card-header"><div><h2>Applications</h2><div class="small muted">Manage OAuth clients, redirect URIs, and access policies.</div></div><button class="btn btn-primary" id="add-application">Add application</button></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Application</th><th>Client ID</th><th>Status</th><th>Redirect URIs</th><th>Allowed groups</th><th>Actions</th></tr></thead><tbody id="application-rows"></tbody></table></div></div></section>
    </div>
  </main>
</div>
<div class="modal-backdrop" id="modal"><div class="modal"><div class="modal-header"><h2 id="modal-title"></h2><button class="close" id="modal-close" aria-label="Close">×</button></div><div id="modal-body"></div></div></div>
<div class="alert hidden" id="alert" role="status"></div>
<script>
const state={users:[],groups:[],applications:[]};
const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[char]));
const date=value=>new Intl.DateTimeFormat('en-GB',{dateStyle:'medium',timeStyle:'short',timeZone:'Asia/Jakarta'}).format(new Date(value));
const request=async(path,options={})=>{const response=await fetch(path,{...options,headers:{'Content-Type':'application/json',...(options.headers||{})}});if(response.status===204)return null;const body=await response.json().catch(()=>null);if(!response.ok)throw new Error(body?.error?.message||'The request could not be completed.');return body};
const notify=(message,error=false)=>{const el=document.getElementById('alert');el.textContent=message;el.className='alert'+(error?' error':'');setTimeout(()=>el.classList.add('hidden'),3500)};
const closeModal=()=>document.getElementById('modal').classList.remove('open');
const openModal=(title,html,onSubmit)=>{document.getElementById('modal-title').textContent=title;document.getElementById('modal-body').innerHTML=html;document.getElementById('modal').classList.add('open');const form=document.querySelector('#modal-body form');if(form)form.addEventListener('submit',async event=>{event.preventDefault();const submit=form.querySelector('[type=submit]');submit.disabled=true;try{const shouldClose=await onSubmit(new FormData(form));if(shouldClose!==false){closeModal();await loadAll()}}catch(error){notify(error instanceof Error?error.message:'The request could not be completed.',true)}finally{submit.disabled=false}})};
const loadAll=async()=>{document.getElementById('content').classList.add('loading');try{[state.users,state.groups,state.applications]=await Promise.all([request('/users'),request('/groups'),request('/applications')]);renderAll()}catch(error){notify(error instanceof Error?error.message:'Unable to load Control Panel data.',true)}finally{document.getElementById('content').classList.remove('loading')}};
const renderAll=()=>{document.getElementById('active-users').textContent=state.users.filter(user=>user.status==='ACTIVE').length;document.getElementById('total-users').textContent=state.users.length+' total users';document.getElementById('group-count').textContent=state.groups.length;document.getElementById('active-apps').textContent=state.applications.filter(app=>app.status==='ACTIVE').length;document.getElementById('total-apps').textContent=state.applications.length+' total applications';renderUsers();renderGroups();renderApplications();document.getElementById('policy-overview').innerHTML=state.applications.length?state.applications.map(app=>'<div class="check-row"><div><strong>'+esc(app.name)+'</strong><div class="small muted">'+esc(app.clientId)+'</div></div><div>'+((app.policies||[]).map(policy=>'<span class="badge neutral-badge">'+esc(policy.groupName)+'</span>').join(' ')||'<span class="muted">No group has access</span>')+'</div></div>').join(''):'<div class="empty">No applications registered.</div>'};
const renderUsers=()=>{const query=document.getElementById('user-search').value.toLowerCase();const status=document.getElementById('user-filter').value;const users=state.users.filter(user=>(!status||user.status===status)&&(!query||user.name.toLowerCase().includes(query)||user.email.toLowerCase().includes(query)));document.getElementById('user-rows').innerHTML=users.length?users.map(user=>'<tr><td><strong>'+esc(user.name)+'</strong></td><td>'+esc(user.email)+'</td><td><span class="badge '+(user.status==='ACTIVE'?'active-badge':'inactive-badge')+'">'+(user.status==='ACTIVE'?'Active':'Inactive')+'</span></td><td>'+date(user.createdAt)+'</td><td><div class="actions"><button class="btn btn-sm" data-user-edit="'+user.id+'">Edit</button><button class="btn btn-sm" data-user-password="'+user.id+'">Change password</button><button class="btn btn-sm '+(user.status==='ACTIVE'?'btn-danger':'')+'" data-user-status="'+user.id+'">'+(user.status==='ACTIVE'?'Deactivate':'Activate')+'</button></div></td></tr>').join(''):'<tr><td colspan="5" class="empty">No users match this view.</td></tr>'};
const renderGroups=()=>{document.getElementById('group-rows').innerHTML=state.groups.length?state.groups.map(group=>'<tr><td><strong>'+esc(group.name)+'</strong></td><td>'+group.memberCount+'</td><td>'+date(group.createdAt)+'</td><td><div class="actions"><button class="btn btn-sm" data-group-edit="'+group.id+'">Rename</button><button class="btn btn-sm" data-group-members="'+group.id+'">Manage members</button></div></td></tr>').join(''):'<tr><td colspan="4" class="empty">No groups created.</td></tr>'};
const renderApplications=()=>{document.getElementById('application-rows').innerHTML=state.applications.length?state.applications.map(app=>'<tr><td><strong>'+esc(app.name)+'</strong></td><td class="mono small">'+esc(app.clientId)+'</td><td><span class="badge '+(app.status==='ACTIVE'?'active-badge':'inactive-badge')+'">'+(app.status==='ACTIVE'?'Active':'Inactive')+'</span></td><td>'+app.redirectUris.length+'</td><td>'+app.policies.length+'</td><td><div class="actions"><button class="btn btn-sm" data-app-edit="'+app.id+'">Edit</button><button class="btn btn-sm" data-app-policy="'+app.id+'">Manage access</button><button class="btn btn-sm '+(app.status==='ACTIVE'?'btn-danger':'')+'" data-app-status="'+app.id+'">'+(app.status==='ACTIVE'?'Deactivate':'Activate')+'</button></div></td></tr>').join(''):'<tr><td colspan="6" class="empty">No applications registered.</td></tr>'};
const userForm=(user=null)=>'<form><div class="field"><label for="user-name">Name</label><input class="input" id="user-name" name="name" maxlength="191" required value="'+esc(user?.name||'')+'"></div><div class="field"><label for="user-email">Email</label><input class="input" id="user-email" name="email" type="email" maxlength="254" required value="'+esc(user?.email||'')+'"></div>'+(!user?'<div class="field"><label for="user-password">Temporary password</label><input class="input" id="user-password" name="password" type="password" minlength="8" maxlength="128" required><div class="field-help">Use at least 8 characters. The password is stored as a secure hash.</div></div>':'')+'<div class="modal-footer"><button type="button" class="btn" data-cancel>Cancel</button><button type="submit" class="btn btn-primary">'+(user?'Save changes':'Create user')+'</button></div></form>';
const openUser=(user=null)=>openModal(user?'Edit user':'Add user',userForm(user),async data=>{const payload={name:data.get('name'),email:data.get('email')};if(!user)payload.password=data.get('password');await request(user?'/users/'+user.id:'/users',{method:user?'PATCH':'POST',body:JSON.stringify(payload)});notify(user?'User updated.':'User created.')});
const openPassword=user=>openModal('Change password','<form><p class="muted">Set a new password for <strong>'+esc(user.name)+'</strong>. Existing sessions will be revoked.</p><div class="field"><label for="new-password">New password</label><input class="input" id="new-password" name="password" type="password" minlength="8" maxlength="128" required></div><div class="modal-footer"><button type="button" class="btn" data-cancel>Cancel</button><button type="submit" class="btn btn-primary">Change password</button></div></form>',async data=>{await request('/users/'+user.id+'/password',{method:'PATCH',body:JSON.stringify({password:data.get('password')})});notify('Password changed and existing sessions revoked.')});
const groupForm=group=>'<form><div class="field"><label for="group-name">Group name</label><input class="input" id="group-name" name="name" maxlength="191" required value="'+esc(group?.name||'')+'"></div><div class="modal-footer"><button type="button" class="btn" data-cancel>Cancel</button><button type="submit" class="btn btn-primary">'+(group?'Save changes':'Create group')+'</button></div></form>';
const openGroup=group=>openModal(group?'Rename group':'Add group',groupForm(group),async data=>{await request(group?'/groups/'+group.id:'/groups',{method:group?'PATCH':'POST',body:JSON.stringify({name:data.get('name')})});notify(group?'Group renamed.':'Group created.')});
const openMembers=async group=>{try{const detail=await request('/groups/'+group.id);const memberIds=new Set(detail.members.map(user=>user.id));const rows=state.users.map(user=>'<div class="check-row"><label><input type="checkbox" name="member" value="'+user.id+'" '+(memberIds.has(user.id)?'checked':'')+'> <span><strong>'+esc(user.name)+'</strong><span class="small muted" style="display:block">'+esc(user.email)+'</span></span></label></div>').join('')||'<div class="empty">Create a user before assigning members.</div>';openModal('Members of '+group.name,'<form><p class="muted">Select every user who should belong to this group.</p><div class="checklist">'+rows+'</div><div class="modal-footer"><button type="button" class="btn" data-cancel>Cancel</button><button type="submit" class="btn btn-primary">Save members</button></div></form>',async data=>{const selected=new Set(data.getAll('member'));const tasks=[];for(const user of state.users){if(selected.has(user.id)&&!memberIds.has(user.id))tasks.push(request('/groups/'+group.id+'/users',{method:'POST',body:JSON.stringify({userId:user.id})}));if(!selected.has(user.id)&&memberIds.has(user.id))tasks.push(request('/groups/'+group.id+'/users/'+user.id,{method:'DELETE'}))}await Promise.all(tasks);notify('Group membership updated.')})}catch(error){notify(error instanceof Error?error.message:'Unable to load group members.',true)}};
const redirectFields=values=>values.map(value=>'<div class="row redirect-row" style="margin-bottom:8px"><input class="input" name="redirectUri" type="url" required value="'+esc(value)+'"><button type="button" class="btn btn-danger" data-remove-redirect>Remove</button></div>').join('');
const appForm=app=>'<form><div class="field"><label for="app-name">Application name</label><input class="input" id="app-name" name="name" maxlength="191" required value="'+esc(app?.name||'')+'"></div>'+(!app?'<div class="field"><label for="client-id">Client ID</label><input class="input" id="client-id" name="clientId" minlength="3" maxlength="191" pattern="[A-Za-z0-9._-]+" required><div class="field-help">Use letters, numbers, dots, underscores, or hyphens.</div></div><div class="field"><label for="client-secret">Client secret</label><input class="input" id="client-secret" name="clientSecret" minlength="24" maxlength="128" required><div class="field-help">This value is returned only when the application is created.</div></div>':'')+'<div class="field"><label>Redirect URIs</label><div id="redirect-fields">'+redirectFields(app?.redirectUris||[''])+'</div><button type="button" class="btn btn-sm" id="add-redirect">Add redirect URI</button></div><div class="field"><label for="launch-url">Launch URL</label><input class="input" id="launch-url" name="launchUrl" type="url" value="'+esc(app?.launchUrl||'')+'"><div class="field-help">Optional address used to open the application.</div></div><div class="field"><label for="logout-url">Logout notification URL</label><input class="input" id="logout-url" name="logoutNotificationUrl" type="url" required value="'+esc(app?.logoutNotificationUrl||'')+'"></div><div class="modal-footer"><button type="button" class="btn" data-cancel>Cancel</button><button type="submit" class="btn btn-primary">'+(app?'Save changes':'Register application')+'</button></div></form>';
const openApplication=app=>{openModal(app?'Edit application':'Add application',appForm(app),async data=>{const payload={name:data.get('name'),redirectUris:data.getAll('redirectUri'),launchUrl:data.get('launchUrl')||null,logoutNotificationUrl:data.get('logoutNotificationUrl')};if(!app){payload.clientId=data.get('clientId');payload.clientSecret=data.get('clientSecret')}const result=await request(app?'/applications/'+app.id:'/applications',{method:app?'PATCH':'POST',body:JSON.stringify(payload)});if(result?.clientSecret){await loadAll();notify('Application registered. Copy the client secret now.');openModal('Application registered','<p>The client secret is shown only once. Store it in the application environment.</p><div class="secret mono">'+esc(result.clientSecret)+'</div><div class="modal-footer"><button type="button" class="btn btn-primary" data-cancel>Done</button></div>',async()=>{});return false}else notify('Application updated.')});document.getElementById('add-redirect').onclick=()=>document.getElementById('redirect-fields').insertAdjacentHTML('beforeend',redirectFields(['']))};
const openPolicies=app=>{const assigned=new Set(app.policies.map(policy=>policy.groupId));const rows=state.groups.map(group=>'<div class="check-row"><label><input type="checkbox" name="policy" value="'+group.id+'" '+(assigned.has(group.id)?'checked':'')+'> '+esc(group.name)+'</label></div>').join('')||'<div class="empty">Create a group before assigning access.</div>';openModal('Access to '+app.name,'<form><p class="muted">Select the groups allowed to sign in to this application.</p><div class="checklist">'+rows+'</div><div class="modal-footer"><button type="button" class="btn" data-cancel>Cancel</button><button type="submit" class="btn btn-primary">Save access</button></div></form>',async data=>{const selected=new Set(data.getAll('policy'));const tasks=[];for(const group of state.groups){if(selected.has(group.id)&&!assigned.has(group.id))tasks.push(request('/applications/'+app.id+'/policies',{method:'POST',body:JSON.stringify({groupId:group.id})}));if(!selected.has(group.id)&&assigned.has(group.id))tasks.push(request('/applications/'+app.id+'/policies/'+group.id,{method:'DELETE'}))}await Promise.all(tasks);notify('Application access updated.')})};
document.querySelectorAll('[data-section]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-section]').forEach(item=>item.classList.remove('active'));document.querySelectorAll('.section').forEach(section=>section.classList.remove('active'));button.classList.add('active');document.getElementById(button.dataset.section).classList.add('active');document.getElementById('page-title').textContent=button.textContent}));
document.getElementById('modal-close').onclick=closeModal;document.getElementById('modal').addEventListener('click',event=>{if(event.target.id==='modal')closeModal()});document.addEventListener('click',async event=>{const target=event.target.closest('button');if(!target)return;if(target.matches('[data-cancel]'))closeModal();if(target.matches('[data-remove-redirect]')){const rows=document.querySelectorAll('.redirect-row');if(rows.length>1)target.closest('.redirect-row').remove();else notify('At least one redirect URI is required.',true)}const user=state.users.find(item=>item.id===(target.dataset.userEdit||target.dataset.userPassword||target.dataset.userStatus));if(target.dataset.userEdit)openUser(user);if(target.dataset.userPassword)openPassword(user);if(target.dataset.userStatus&&user){try{await request('/users/'+user.id+'/status',{method:'PATCH',body:JSON.stringify({status:user.status==='ACTIVE'?'INACTIVE':'ACTIVE'})});notify(user.status==='ACTIVE'?'User deactivated.':'User activated.');await loadAll()}catch(error){notify(error.message,true)}}const group=state.groups.find(item=>item.id===(target.dataset.groupEdit||target.dataset.groupMembers));if(target.dataset.groupEdit)openGroup(group);if(target.dataset.groupMembers)openMembers(group);const app=state.applications.find(item=>item.id===(target.dataset.appEdit||target.dataset.appPolicy||target.dataset.appStatus));if(target.dataset.appEdit)openApplication(app);if(target.dataset.appPolicy)openPolicies(app);if(target.dataset.appStatus&&app){try{await request('/applications/'+app.id,{method:'PATCH',body:JSON.stringify({status:app.status==='ACTIVE'?'INACTIVE':'ACTIVE'})});notify(app.status==='ACTIVE'?'Application deactivated.':'Application activated.');await loadAll()}catch(error){notify(error.message,true)}}});
document.getElementById('add-user').onclick=()=>openUser();document.getElementById('add-group').onclick=()=>openGroup();document.getElementById('add-application').onclick=()=>openApplication();document.getElementById('user-search').addEventListener('input',renderUsers);document.getElementById('user-filter').addEventListener('change',renderUsers);loadAll();
</script>
</body>
</html>`;
}
